import { Poppins, Kanit } from "next/font/google";

// Next fonts
export const poppins = Poppins({
  subsets: ['latin'],
  weight: ['400']
})

export const kanit = Kanit({
  subsets: ['latin'],
  weight: ['400']
})